spring:
  application:
    name: product-server
  data:
    mongodb:
      uri: mongodb://localhost:27017/product-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8083 # random port on startup